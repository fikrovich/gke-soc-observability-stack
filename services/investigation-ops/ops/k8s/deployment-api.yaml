apiVersion: apps/v1
kind: Deployment
metadata:
  name: investigation-ops-api
  namespace: investigations
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: investigation-ops
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: investigation-ops
        app.kubernetes.io/component: api
    spec:
      serviceAccountName: investigation-ops-api
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api
          image: ghcr.io/example/gke-soc-observability-stack/investigation-ops:20260314-210422
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "app.api.main"]
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: investigation-ops-config
            - secretRef:
                name: investigation-ops-secret
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
