apiVersion: v1
kind: Service
metadata:
  name: edge-ingest
  namespace: observability
  labels:
    app: edge-ingest
spec:
  type: LoadBalancer
  selector:
    app: edge-ingest-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
